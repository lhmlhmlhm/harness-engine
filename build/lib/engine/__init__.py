"""harness-engine — a domain-free flow engine.

The version lives here and nowhere else. `pyproject.toml` reads it through
`[tool.setuptools.dynamic]`, so there is one place to change and no way for the packaged
metadata to disagree with the code it packages.
"""

__version__ = "0.1.0"
