#!/usr/bin/env python3
"""harness — entry point. Kept trivial so all behaviour lives in engine/."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from engine.harness import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
